/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/JK_TEST',
    assetPrefix: '/JK_TEST/',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
